import Page from "../components/page";

export default function Index() {
  return (
    <Page>
      A beautiful landing page with impressive motifs that makes you want to
      have one
    </Page>
  );
}

// Index.getInitialProps = authMiddleware;
